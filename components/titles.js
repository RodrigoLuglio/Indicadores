export const BlockTitle = ({children}) => {
    return (
        <h2 className="font-gotham_bold text-paragraph text-lg">{children}</h2>
    );
}


export const SubBlockTitle = ({children}) => {
    return (
        <h3 className="font-gotham_bold text-green_light text-[15px]">{children}</h3>
    );
}